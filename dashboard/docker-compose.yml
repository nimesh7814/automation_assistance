services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "8000:8000"
    restart: unless-stopped

  ui:
    build:
      context: .
      dockerfile: ui/Dockerfile
    environment:
      API_BASE_URL: http://api:8000
    env_file:
      - path: ./ui/.env
        required: false
    ports:
      - "8501:8501"
    depends_on:
      - api
    restart: unless-stopped
